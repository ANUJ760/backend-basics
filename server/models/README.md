# Models Guide

- `User` stores account data.
- `todo` stores todo data.
- MongoDB auto-generates `_id` for each document.
- `_id` type is usually `ObjectId`.
- Mongoose is mostly used here because model files define schema rules and DB access structure.
