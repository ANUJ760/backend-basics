# Routes Guide

Routes map HTTP method + URL to controller functions.

Examples:
- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/todos`
- `POST /api/todos`
- `PUT /api/todos/:id`
- `DELETE /api/todos/:id`

REST and CRUD chapter files are placed in this folder.
