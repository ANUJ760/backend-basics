# Controllers Guide

Controllers contain business logic:
- validate inputs
- call models
- return status codes and JSON responses
- handle edge cases (duplicate email, invalid login, missing todo, ownership checks)
